'use strict';

module.exports = {

    kubernetes: {
        apiHost: 'https://kubernetes.default' //NOTE: this is not a variable, a kubernetes service will always be set in the default namespace
    }
};
